# Pyodide cross-build / test artifacts
# node_modules is regenerated by `npm install` (package.json + package-lock.json
# are tracked); never commit it (it vendors multi-MB pyodide wasm binaries).
/node_modules/
default.profraw
